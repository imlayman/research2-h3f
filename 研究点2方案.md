# 一、方法总览：H3F-Recon

方案名：

## **H3F-Recon**

**Hierarchical Hybrid Hash-Feature Reconstruction for Large-Scale Point Clouds**

核心思想是四句话：
* **表示层**：不用全局单 SDF，而是用**层次稀疏网格 + 局部隐式子场**表示整个场景。
* **编码层**：每个查询点只读取**局部多尺度特征**，而不是和全场景交互。
* **训练层**：用**几何监督 + 局部一致性 + 跨块融合约束**联合优化。
* **推理层**：采用**候选块检索 + 局部解码 + 自适应抽取**，避免全局密集 marching cubes。

这个设计本质上吸收了几条成熟路线的优点：

* 向 **NKSR** 借鉴“稀疏层次表示”的可扩展性；
* 向 **SHINE-Mapping** 借鉴“局部特征存储、共享小解码器”的省显存思想；
* 向 **NoKSR** 借鉴“多尺度局部查询和高效邻域访问”的速度优势；
* 向 **local SDF overlap + blending** 借鉴“块间重叠融合”的无缝拼接思路。

---

# 二、表示设计：全局稀疏层次骨架 + 局部连续隐式子场

## 2.1 全局不是一个 SDF，而是一个“场图”

整个场景先建立一个**层次稀疏空间索引**，可以是八叉树或者稀疏哈希层次网格。这样做不是拍脑袋，而是因为大规模重建里，八叉树/稀疏层次结构已经被证明比稠密体素更适合控制内存，并支持增量和局部访问。SHINE-Mapping 就是用八叉树层次稀疏特征来提升大规模场景的 memory efficiency；NKSR 也是用 sparse voxel hierarchy 承载局部表示。

把每个活跃空间体素记为一个 **cell**，每个 cell 不直接存一个完整网格，而是存：

* 一个**粗层语义/几何特征** (f^g_i)
* 一个**细层局部细节特征** (f^l_i)
* 一个**局部坐标变换参数** (T_i)
* 一个**置信度/质量分数** (q_i)

这里的 (T_i) 很关键，它表示这个局部块在全局中的相对对齐方式。因为大规模场景里，局部块之间往往存在扫描密度差异、法向噪声和视角偏差，只靠简单拼接很容易裂缝。2025 那篇 local SDF fusion 方案专门做了相邻 local SDF 的 relative pose registration，再做 blending。

---

## 2.2 每个局部块不是独立网络，而是“共享解码器 + 局部 latent”

这是我设计里最重要的一点：

### 不做：

* 每个分块一个完整 MLP
  因为这会：
* 参数爆炸
* 泛化变差
* 邻块边界不一致
* 难以端到端训练

### 要做：

* 全局共享一个**轻量隐式解码器** (D_\theta)
* 每个局部块只提供少量 latent feature 和坐标条件

也就是：
[
s(x)=D_\theta(\phi(x),, \mathcal{F}(x))
]
其中：

* (x) 是查询点
* (\phi(x)) 是位置编码/局部相对坐标编码
* (\mathcal{F}(x)) 是从其附近若干局部块插值得到的多尺度特征
* 输出 (s(x)) 是 SDF 或 UDF 值

这种“**局部特征存储 + 共享小解码器**”和 SHINE-Mapping 的思路一致，它们就是把特征存储在层次稀疏结构中，再用一个 shallow network 解码。这样能显著减小参数量和显存压力。

---

# 三、输入编码：局部多尺度几何编码，而不是全局位置编码

如果要兼顾**泛化**和**速度**，我不会只用普通 Fourier positional encoding。我的编码会分三层：

## 3.1 点级局部几何编码

对于每个查询点 (x)，从周围邻域点云提取：

* 相对坐标 ((p_j-x))
* 局部法向/估计法向
* 局部密度
* 局部 PCA 主方向/曲率 proxy
* 与最近切平面的距离残差

这一步的目的，是让网络学到“局部几何模式”，而不是死记具体形状。因为局部先验比整体形状更容易泛化，这和 Points2Surf、LoSF-UDF 那类方法的基本逻辑一致：**局部表面模式更稳定、更可迁移。**

## 3.2 cell 级多尺度特征聚合

查询点不只读一个块，而是读：

* 粗层 cell：保证全局连续性
* 中层 cell：恢复主体几何
* 细层 cell：补充高频细节

这一步受 NoKSR 启发。NoKSR 强调**多尺度序列化和多尺度聚合**对于补偿近似邻域检索误差是关键的。虽然我这里不一定照搬它的序列化形式，但“**多尺度局部聚合**”一定要保留。

## 3.3 自适应频率编码

高频编码只在曲率高、边缘复杂、法向变化大的区域增强；在大平面、低曲率区域只用低频编码。
原因很直接：大规模场景大部分区域其实不需要高频特征。如果全空间一视同仁地上高频编码，会浪费显存、减慢训练，还容易过拟合噪声。

这一步是我对现有方法的一个增强点：
**不是整个场景都用高频特征隐式表示，而是把资源预算投到复杂区域。**

---

# 四、核心网络：双分支隐式解码

## 4.1 主干：局部连续场分支

主干分支预测：
[
s_i(x) = D_\theta(x_{rel}, f_i^{multi})
]
表示点 (x) 在第 (i) 个候选局部场中的 SDF/UDF。

候选局部场不会很多，只取覆盖该点的 top-k 个重叠块，比如 2~4 个。这样一来，查询复杂度是**局部常数级**，而不是随全场景增长。这个设计和大规模方法强调的“只访问局部、避免全局交互”是一致的。

## 4.2 辅助：融合权重分支

同时增加一个权重预测分支：
[
w_i(x)=G_\psi(x_{rel}, f_i^{multi}, q_i)
]
满足：
[
\sum_i w_i(x)=1,\quad w_i(x)\ge0
]

最终隐式值：
[
s(x)=\sum_i w_i(x), s_i(x)
]

这一步其实就是把 **local SDF blending** 神经化、可学习化。
它的意义非常大：
不是“先分块、后硬拼”，而是**在隐式函数值层面连续融合**。这和 2025 那篇 divide-and-conquer local SDF 的思想高度一致，只是我把 blending 做成一个端到端学习的模块，而不是后处理规则。

## 4.3 一个额外输出：不确定性

解码器再输出一个不确定性：
[
u(x)
]
用于：

* 推理时控制自适应采样
* 抽网格时局部加密
* 后处理时抑制噪声区域伪表面

因为真实大规模点云里，空洞、稀疏、噪声区域非常常见。综述和 benchmark 也指出，学习方法面对真实采集缺陷时仍有不稳定问题。引入 uncertainty 可以提升工程稳健性。

---

# 五、训练策略：端到端，但监督不只一个 SDF loss

我把训练损失设计成“五类损失”。

## 5.1 表面一致性损失

对点云表面点 (p)：
[
\mathcal{L}_{surf}=|s(p)|
]
让采样点落在零水平集附近。

## 5.2 近表面符号/距离监督

在表面附近沿法向或估计法向采样正负偏移点：
[
p^\pm = p \pm \epsilon n
]
约束：
[
s(p^+) > 0,\quad s(p^-) < 0
]
或者用截断 SDF 回归。

这一步是常见 SDF 训练套路，但对大规模点云不能太依赖高质量 GT normal，所以允许用**鲁棒法向估计 + soft sign supervision**。这样更适合真实扫描数据。

## 5.3 Eikonal/梯度正则

[
\mathcal{L}_{eik} = \left(|\nabla_x s(x)|-1\right)^2
]
帮助隐式场保持局部几何合理性。
很多 SDF 方法都依赖这类正则保证表面性质，不过单独依赖它不够，所以只把它当辅助项。2025 的 scalable local SDF 文章也提到现有 neural SDF 方法普遍要处理这类 regularization 问题。

## 5.4 块间接缝一致性损失

这是重点强调的创新点之一。

在重叠区域 (\Omega_{ij}) 内，对相邻两个局部场：
[
\mathcal{L}_{seam}=|s_i(x)-s_j(x)| + \lambda |\nabla s_i(x)-\nabla s_j(x)|
]

也就是不仅函数值要一致，梯度也要一致。
这样能抑制：

* 值跳变
* 法向突变
* 抽网格后出现裂缝或折痕

这一步其实是把“blending”进一步前移到训练阶段：
**不是等推理时再补救，而是在训练时强迫相邻子场学成连续的。**

## 5.5 局部-全局一致性蒸馏损失

我还会再加一个全局 coarse field，分辨率较低，用来预测一个粗 SDF：
[
s^{coarse}(x)
]
并让局部融合结果 (s(x)) 和全局粗场在大尺度上一致：
[
\mathcal{L}_{cg}=| \text{stopgrad}(s^{coarse}(x)) - s(x) |
]

它的作用是防止局部块“各自为战”，特别是在稀疏区域和遮挡严重区域。
也就是：

* 局部块负责细节
* 全局粗场负责整体几何趋势

---

# 六、怎么解决最关心的四个问题

## 6.1 降显存

### 方案核心

* 只存**活跃空间体素**
* 只在活跃体素里存**低维 latent**
* 用**共享轻量 decoder**
* 查询时只访问局部 top-k 块
* 训练时采用**分块微批 + 邻域缓存**

### 原理

这和 NKSR、SHINE-Mapping 的大方向是一致的：
它们之所以能扩展到大场景，本质原因就是把表示做成**稀疏层次化**，而不是稠密化。NKSR 还特别强调紧支撑核使得求解是 sparse linear solve，从而 memory-efficient；SHINE-Mapping 则强调 octree sparse feature 的 extensible 和 memory-efficient。

### 额外加的工程细节

* **特征量化/半精度存储**
* **低频块与高频块异构维度**：平滑区域 latent 维度更小，复杂区域更大
* **静态块冻结机制**：训练后期对已收敛块停止反传，只更新困难区域
* **active block curriculum**：每轮只激活部分块训练

这样你就不是“整个大场景一起训练”，而是**全局共享参数 + 局部稀疏更新**。

---

## 6.2 提升泛化性

泛化差的核心原因通常不是模型太小，而是**模型学了具体场景分布，而不是几何规律**。

### 采用三招：

### 第一招：局部几何先验学习

训练时不直接以“整场景”为单位，而是大量采样：

* 平面 patch
* 曲面 patch
* 棱边 patch
* 薄结构 patch
* 稀疏/噪声/缺失 patch

这样模型首先学会**局部几何构型**。这和局部方法比全局方法更容易泛化的结论一致。

### 第二招：多域混合训练

训练集故意混合：

* 室内
* 室外
* 单物体
* 建筑/街景
* 稠密扫描
* 稀疏 LiDAR

NKSR 明确提到它能从混合 objects 和 scenes、不同尺度数据里学习，并保持 generalization。这个结论对你的研究点特别重要：**不要只在一个窄数据域里学。**

### 第三招：法向依赖降级

真实大规模点云里法向往往不可靠，所以我不会把法向当成强监督真值，而是当成：

* 可选辅助输入
* soft regularizer
* 或只在高置信区域用

这样跨数据源泛化会更稳。

---

## 6.3 提升重建速度

速度慢主要卡在三处：

1. 邻域检索慢
2. 隐式查询次数太多
3. 网格提取太密

### 分别处理：

### 第一：候选块检索 O(1) 化

借助稀疏层次索引，查询点直接定位到少数几个 active cells，避免全局 KNN。
这和 NoKSR 用序列化实现 fast approximate neighbors retrieval 的目标是一致的：都是在解决“大规模邻域访问成本”问题。

### 第二：只在近表面做密集查询

对远离表面的区域，不需要精细 SDF。
先做一个粗 occupancy/signedness 预测，只有“可能接近表面”的区域才做高精度 SDF 解码。

### 第三：自适应网格提取

不是全局 dense marching cubes，而是：

* 先 coarse scan
* 只在符号变化附近细分
* 在 uncertainty 高/曲率高区域再局部 refine

这会显著减少查询次数。


## 6.4 解决分块接缝

这是我最确定的一部分，因为已有工作已经给出方向了：
**靠 overlap + registration + blending。**

把它做得更完整：

### 第一步：块间必须重叠

不是无缝拼 tile，而是每个块边界向外扩一圈，保证相邻块有共享上下文。

### 第二步：局部坐标自对齐

每个块学一个小的局部刚性/仿射校正参数 (T_i)，相邻块在 overlap 区先对齐。

### 第三步：值和梯度双一致性

训练时在 overlap 区约束：

* SDF 值一致
* gradient 一致

### 第四步：融合权重连续

融合分支输出的 (w_i(x)) 必须空间连续，不能出现硬切换。
加一个 smoothness regularizer：
[
\mathcal{L}_{blend} = |\nabla_x w_i(x)|
]
避免权重突变导致 seam。

### 第五步：推理阶段只输出融合后的单一场

不是分别对每块抽 mesh 再拼，而是先融合成统一隐式场，再抽网格。
这是避免几何裂缝的关键。

---

