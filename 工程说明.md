# 研究点2代码工程说明（H3F-Recon MVP）

## 1. 工程目标
本工程是“基于层次稀疏表示与局部隐式融合的大规模点云表面重建方法”的最小可运行骨架，支持：
- YAML 配置驱动
- 训练日志（stdout + runs 目录文件）
- checkpoint 保存/加载（含模型参数与稀疏特征参数）
- dummy 随机点云 smoke test
- 推理导出近表面点（PLY + NPY）

## 2. 目录结构与文件职责

```text
.
├─ configs/
│  └─ mvp.yaml
├─ h3f_recon/
│  ├─ __init__.py
│  ├─ config.py
│  ├─ data/
│  │  ├─ __init__.py
│  │  └─ dummy_dataset.py
│  ├─ models/
│  │  ├─ __init__.py
│  │  ├─ positional_encoding.py
│  │  ├─ sparse_hierarchy.py
│  │  ├─ decoder.py
│  │  └─ h3f_recon.py
│  ├─ engine/
│  │  ├─ __init__.py
│  │  └─ losses.py
│  └─ utils/
│     ├─ __init__.py
│     ├─ logger.py
│     ├─ checkpoint.py
│     ├─ misc.py
│     └─ io.py
├─ train.py
├─ infer.py
└─ 研究点2方案.md
```

### 2.1 配置与入口
- `configs/mvp.yaml`：统一实验配置（data/model/train/infer）。
- `train.py`：训练入口，创建 run 目录、日志、checkpoint、支持 resume。
- `infer.py`：推理入口，加载 checkpoint 后查询隐式场并导出结果。

### 2.2 核心包 `h3f_recon`
- `config.py`：dataclass 配置定义、YAML 解析与保存。

#### data
- `dummy_dataset.py`：生成随机球体点云及监督样本（surface/near-surface/eikonal），用于 smoke test。

#### models
- `positional_encoding.py`：局部坐标 Fourier 编码。
- `sparse_hierarchy.py`：层次稀疏特征参数（feature/quality/translation）与候选块检索。
- `decoder.py`：共享解码网络（SDF、融合权重分支、不确定性分支）和 coarse field。
- `h3f_recon.py`：总模型封装，完成 top-k 局部融合得到最终 SDF。

#### engine
- `losses.py`：训练损失集合：
  - `surface`（表面一致性）
  - `sign`（近表面距离监督）
  - `eikonal`（梯度正则）
  - `seam`（局部子场接缝一致性）
  - `coarse_global`（局部-全局一致性）
  - `blend_smooth`（融合权重平滑）

#### utils
- `logger.py`：同时输出到 stdout 和日志文件。
- `checkpoint.py`：保存/加载训练状态（包含 `sparse_feature_state`）。
- `misc.py`：设备选择、随机种子、run 目录创建、batch 上设备。
- `io.py`：点云保存（优先 open3d，fallback ASCII PLY）。

## 3. 运行方式

## 3.1 环境
- Python 3.10+
- PyTorch 2.x
- 依赖建议：`numpy torch tqdm pyyaml`（`open3d` 可选）

安装示例：
```bash
pip install numpy torch tqdm pyyaml
# 可选
pip install open3d
```

## 3.2 训练
```bash
python train.py --config configs/mvp.yaml
```

快速 smoke test：
```bash
python train.py --config configs/mvp.yaml --max-steps 2
```

可选参数：
- `--run-name` 覆盖 `train.run_name`
- `--resume` 从 checkpoint 恢复
- `--device` 指定 `cpu/cuda`

产物位置：
- `runs/train_<run_name>_<timestamp>/train.log`
- `runs/train_<run_name>_<timestamp>/ckpt_latest.pt`

## 3.3 推理
```bash
python infer.py --config configs/mvp.yaml --checkpoint runs/train_xxx/ckpt_latest.pt
```

可选参数：
- `--grid-resolution` 网格采样分辨率
- `--iso-threshold` 近表面阈值
- `--query-batch-size` 查询批大小

产物位置：
- `runs/infer_<run_name>_<timestamp>/infer.log`
- `recon_surface_sdf_grid.npy`
- `recon_surface_points.npy`
- `recon_surface.ply`

## 4. 说明
- 当前骨架默认仅实现 `data.mode=dummy`，便于先打通训练/推理全链路。
- 后续可在 `h3f_recon/data/` 扩展真实数据集读取与预处理模块。
